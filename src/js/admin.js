// TODO Milstolpe 2: Admin-funktionalitet
// - Inloggningsformulär mot POST /api/login
// - Spara JWT-token i sessionStorage
// - Hämta produkter via GET /api/produkter
// - Lägg till produkt via POST /api/produkter (JWT krävs)
// - Redigera produkt via PUT /api/produkter/:id (JWT krävs)
// - Ta bort produkt via DELETE /api/produkter/:id (JWT krävs)
